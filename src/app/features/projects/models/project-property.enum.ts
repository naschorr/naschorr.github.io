export enum ProjectProperty {
    // Just maps a subset of Project model properties to strings. Handy for the filtering logic while avoiding magic strings.
    FLAVOR = "flavor",
    TECHNOLOGIES = "technologies",
}